#include <iostream>
#include <cstdio>
#include <queue>

using namespace std;

queue <pair<int, int>> q;

void sol(int lhs , int rhs)
{
	while(!q.empty()) {

	}
}

int main(int argc, char** argv)
{
	int test_case;
	int T;
	freopen("1267_input.txt", "r", stdin);
	cin>>T;
	/*
	   여러 개의 테스트 케이스가 주어지므로, 각각을 처리합니다.
	*/
	for(test_case = 1; test_case <= T; ++test_case)
	{

	}
	return 0;//정상종료시 반드시 0을 리턴해야합니다.
}