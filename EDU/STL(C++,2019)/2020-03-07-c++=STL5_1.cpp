#include <iostream>
#include <vector>

using namespace std;
#define TEST    (1)
// Container 
// STL Container 종류
/*
    * Sequence Container
        * 요소가 삽입 된 순서대로 놓여 있는 컨테이너
            * List, vectopr Deque
            * foward_list, array
    * Associative Container
        * 삽입도니 순서와 상관 없이 규칙을 가지고 요소를 보관
            * tree : set, multi-set, map, multi-map
            * hash : unordered_map, unordered_multimap  
    * Container Adapter
        * stack, queue, priority_queue   
*/
// STL Container 특징
/*
    * 값을 보관한다
    * 멈버 함수 뿐 아니라 "member type"이 있다.
    * 반복자를 가지고 있다.
    * **제거와 리턴을 동시에 하지 않는다**
        * back/front/top 함수는 요소를 리턴만 하고 제거 하지 않는다
        * pop_xxx 계열의 함수는 제거만 한다. 리턴하지 않는다
        * 임시객체를 막을 수 있고 예외의 안정성을 강력 보장을 지킬 수 있다.
    * STL 자체에는 예외가 거의 없다. 
*/

#if TEST == 1


int main()
{
    
}

#elif TEST == 2
#elif TEST == 3
#elif TEST == 4
#elif TEST == 5
#elif TEST == 6

#endif
